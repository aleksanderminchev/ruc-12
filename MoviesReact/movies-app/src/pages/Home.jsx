import React from "react";

function Home({ title, description, poster }) {
  return (
    <div
      style={{
        margin: "20px",
        border: "1px solid #ddd",
        padding: "20px",
        borderRadius: "8px",
      }}
    >
            HomePage
    </div>
  );
}

export default Home;
